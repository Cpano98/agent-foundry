# Commands

This directory contains reusable command definitions and automation shortcuts.

Commands are often triggered as slash commands or workflow shortcuts. They complement the broader skill ecosystem.

Examples include:

- `review-pr`
- `generate-issue`
- `summarize-change`
- `validate-docs`

Keep commands lightweight, discoverable, and focused on a single job.
