# Tests

This directory is intended for repository-level validation checks, fixture data, and integration-style examples.

The initial implementation includes a small validation script in `scripts/validate/` and a basic `npm run validate` workflow.
